#!/usr/bin/env bash
node=n04
packetsize=100
ncount=5e6
buffersize=100000

outputs="energy_monitor1.h5 tof_monitor1.h5 neutrons.dat"

rm -rf ${outputs}
ARCS_Moderator2Sample.py -neutron_recorder.packetsize=${packetsize} -ncount=${ncount} -buffer_size=${buffersize} -overwrite-datafiles -journal.info.dsm.Runner

